name: Backfill Laps

on:
  workflow_dispatch:
    inputs:
      limit:
        description: 'Max activities without laps to process (newest first, ~500/day max)'
        required: false
        default: '500'
  schedule:
    # Run daily at 2am AEST (4pm UTC) until all laps are backfilled
    - cron: '0 16 * * *'

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests psycopg2-binary

      - name: Run lap backfill
        env:
          STRAVA_CLIENT_ID: ${{ secrets.STRAVA_CLIENT_ID }}
          STRAVA_CLIENT_SECRET: ${{ secrets.STRAVA_CLIENT_SECRET }}
          STRAVA_REFRESH_TOKEN: ${{ secrets.STRAVA_REFRESH_TOKEN }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          BACKFILL_LIMIT: ${{ github.event.inputs.limit || '500' }}
        run: python .github/scripts/backfill_laps.py
